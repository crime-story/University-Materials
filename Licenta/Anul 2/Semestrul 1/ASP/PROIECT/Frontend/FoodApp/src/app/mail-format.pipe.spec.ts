import { MailFormatPipe } from './mail-format.pipe';

describe('MailFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MailFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
